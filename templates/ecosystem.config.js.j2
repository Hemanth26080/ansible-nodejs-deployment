module.exports = {
  apps: [{
    name: "nodeapp",
    script: "{{ app_dir }}/app.js",
    instances: 1,
    autorestart: true,
    watch: false,
    max_memory_restart: "1G",
    env: {
      NODE_ENV: "production",
      PORT: "{{ app_port }}"
    },
    error_file: "/var/log/nodeapp-err.log",
    out_file: "/var/log/nodeapp-out.log",
    log_date_format: "YYYY-MM-DD HH:mm Z"
  }]
}